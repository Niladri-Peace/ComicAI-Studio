{
  "version": 2,
  "builds": [
    { "src": "api/*.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/$1" }
  ],
  "env": {
    "REPLICATE_API_KEY": "@replicate_api_key"
  }
}
